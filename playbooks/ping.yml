---
- name: Ansible ping module
  hosts: "{{ custom_host }}"
  
  tasks:
    - name: Ping check
      ansible.builtin.ping:

    - name: Print message
      ansible.builtin.debug:
        msg: Test complete
