---
- name: Ansible ping module
  hosts: "{{ custom_host }}"
  vars_files: /gitdata/git/ansible-training/group_vars/all/.vault.yml 
  
  tasks:
    - name: Ping check
      ansible.builtin.ping:

    - name: Print message
      ansible.builtin.debug:
        msg: Test complete
